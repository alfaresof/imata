import 'package:flutter/material.dart';

class LastReports extends StatelessWidget {
  const LastReports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('last Reports'),
    );
  }
}
